{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localize-anything.dev/schemas/provider-real-smoke-expansion-decision.schema.json",
  "title": "Provider Real Smoke Expansion Decision",
  "type": "object",
  "required": ["protocol_version", "schema", "artifact", "status", "provider_scope_expansion_authorized", "new_provider_execution_authorized", "blockers", "forbidden_claims", "provider_or_model_called_by_runtime", "target_files_mutated"],
  "properties": {
    "protocol_version": { "const": "0.1" },
    "schema": { "const": "localize-anything-provider-real-smoke-expansion-decision-v1" },
    "artifact": { "const": "provider-real-smoke-expansion-decision.json" },
    "status": { "enum": ["do_not_expand", "review_before_expansion"] },
    "provider_scope_expansion_authorized": { "const": false },
    "new_provider_execution_authorized": { "const": false },
    "blockers": { "type": "array", "items": { "type": "string" } },
    "forbidden_claims": { "type": "array", "items": { "type": "string" } },
    "provider_or_model_called_by_runtime": { "const": false },
    "target_files_mutated": { "const": false }
  },
  "additionalProperties": true
}
