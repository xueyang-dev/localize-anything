{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localize-anything.dev/schemas/provider-mock-failure-report.schema.json",
  "title": "Localize Anything Provider Mock Failure Report",
  "type": "object",
  "required": ["protocol_version", "schema", "artifact", "status", "scenario", "failures", "provider_or_model_called"],
  "properties": {
    "protocol_version": { "const": "0.1" },
    "schema": { "const": "localize-anything-provider-mock-failure-report-v1" },
    "artifact": { "const": "provider-mock-failure-report.json" },
    "status": { "type": "string" },
    "scenario": { "type": "string" },
    "failures": { "type": "array", "items": { "type": "object" } },
    "provider_or_model_called": { "const": false }
  },
  "additionalProperties": true
}
