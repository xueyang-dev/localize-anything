{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localize-anything.dev/schemas/provider-mock-evidence-report.schema.json",
  "title": "Localize Anything Provider Mock Evidence Report",
  "type": "object",
  "required": ["protocol_version", "schema", "artifact", "status", "scenario", "provider_backed", "forbidden_claims"],
  "properties": {
    "protocol_version": { "const": "0.1" },
    "schema": { "const": "localize-anything-provider-mock-evidence-report-v1" },
    "artifact": { "const": "provider-mock-evidence-report.json" },
    "status": { "type": "string" },
    "scenario": { "type": "string" },
    "provider_backed": { "const": false },
    "forbidden_claims": { "type": "array", "items": { "type": "string" } },
    "provider_or_model_called": { "const": false }
  },
  "additionalProperties": true
}
