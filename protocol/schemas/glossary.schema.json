{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://localize-anything.dev/schemas/glossary.schema.json",
  "type": "object",
  "required": ["protocol_version", "schema", "source_locale", "target_locale", "concepts", "updated_at"],
  "properties": {
    "protocol_version": { "const": "0.1" },
    "schema": { "const": "localize-anything-glossary-v1" },
    "source_locale": { "type": "string", "minLength": 1 },
    "target_locale": { "type": "string", "minLength": 1 },
    "concepts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "source_terms", "behavior", "status", "target"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "source_terms": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 } },
          "behavior": { "enum": ["translate", "preserve"], "description": "Use translate for a concept with a target translation; use preserve when the source term must remain unchanged." },
          "status": { "enum": ["candidate", "locked"], "description": "Set locked only after repository evidence or user confirmation; locked concepts are enforced by localize check." },
          "target": {
            "type": "object",
            "description": "Agent operation: lock a translation by setting target.preferred; preserve a term by setting behavior=preserve and status=locked.",
            "required": ["preferred", "forbidden"],
            "properties": {
              "preferred": { "type": "string" },
              "forbidden": { "type": "array", "items": { "type": "string" } }
            }
          },
          "evidence": { "type": "array" }
        }
      }
    },
    "updated_at": { "type": "string", "format": "date-time" }
  }
}
