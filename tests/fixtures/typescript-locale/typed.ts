import type { Translations } from './types'

// Typed full catalog fixture: values must satisfy the Translations contract.
export const zh: Translations = {
  common: {
    save: '保存',
    welcome: '你好，{name}！',
  },
}
